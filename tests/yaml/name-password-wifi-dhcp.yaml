 network:
  interface:
     name: wlan1
     dhcp: yes
     use-dns: no
     use-mtu: yes
     use-domain: yes
     access-points:
         - ssid-name: "network_ssid_name1"
           password: "test123"

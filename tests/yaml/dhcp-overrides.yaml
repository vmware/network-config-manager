network:
  version: 2
  ethernets:
    test98:
      dhcp4: yes
      dhcp4-overrides:
        route-metric: 100
    test99:
      dhcp4: yes
      dhcp4-overrides:
        route-metric: 200
      dhcp6-overrides:
        use-dns: true
